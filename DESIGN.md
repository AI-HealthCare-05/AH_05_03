# DESIGN.md — 로그인 · 가입 · 첫 실행

세 화면의 설계 문서다. 로그인 관문(`SignInPage`), 가입(`SignUpPage`), 가입 직후 첫 실행 화면(`HomePage` 의 `EmptyHousehold`).

## 0. 이 문서의 권위 범위 — 먼저 읽는다

**이 문서는 디자인 토큰을 정의하지 않는다.** 색·반경·그림자·글자 크기의 정본은 `frontend/src/styles.css` 의 `:root` 이고, 여기서는 인용만 한다. 값이 어긋나면 styles.css 가 이긴다.

그렇게 못박는 이유가 있다. 상위 폴더에 `Team_project/docs/DESIGN.md`(v2.0, 2026-08-12)가 있는데 실제 구현과 다른 시스템이다 — 캔버스 `#F5F5F7` 대 `#f5f7fb`, 액센트 `#0066CC` 대 `#2463eb`, 카드 반경 18px 대 22px. 그 문서는 8월에 멈췄고 styles.css 는 9월에도 대비를 실측해 고쳤다. 세 번째 시스템을 만들지 않으려고 이 문서는 화면 설계만 맡는다.

`AGENTS.md` 는 상위 `Team_project/docs/` 를 이 코드의 문서로 읽는 것을 금지한다. 이 문서는 `project/` 안에 있고 그 규칙을 어기지 않는다.

| 무엇 | 어디 |
|---|---|
| 토큰(색·반경·그림자·크기) | `frontend/src/styles.css` `:root` |
| 화면 구성·카피·상태 | 이 문서 |
| 저장 위치·열람 범위 사실관계 | `docs/adr/0011-postgresql-health-data-and-server-ai.md` |
| 질환 목록 | `app/services/assessment.py` 의 `SPECS` |

---

## 1. 지금 무엇이 문제인가

취향이 아니라 코드에서 확인한 것만 적는다.

### 1.1 로그인 화면은 같은 말을 두 번 한다

관문이 패널 제목과 카드 제목을 둘 다 그린다.

```
h1   로그인하고 시작하세요
p    질환 예측과 검진표 인식은 서비스 계정이 있어야 씁니다…
     ┌─ 카드 ────────────────────────┐
     │ kicker  서비스 계정            │
     │ h2      로그인                 │  ← h1 과 같은 말
     │ p       가입하신 이메일로       │  ← 위 p 와 겹친다
     │         로그인하세요.          │
     └───────────────────────────────┘
```

제목 둘·설명 둘이 480px 한 칼럼에 쌓인다. 눈이 멈출 곳이 넷이라 어느 것도 첫 줄로 읽히지 않는다.

### 1.2 글자가 팀이 정한 바닥 아래에 있다

styles.css 의 사다리 주석은 이렇게 적어 뒀다.

> 12px 이하는 두지 않는다 — 이 화면의 주 사용자가 30~60 대이고 검진 수치를 **옮겨 적는** 화면이라, 잘못 읽으면 판정 자체가 틀어진다.

그 사다리는 판정 화면 전용이라고 스스로 범위를 못박았다. 그런데 사용자는 같은 사람이고, 로그인 화면은 그 바닥 아래로 내려가 있다.

| 자리 | 지금 | 사다리 기준 |
|---|---|---|
| `.signin-lead` 리드 문장 | 0.88rem (14px) | 문장 바닥 15px — 미달 |
| `.signin-notes` 개인정보 3줄 | 0.78rem (12.5px) | 12px 이하 금지 — 미달 |
| `.signin-brand small` | 0.78rem (12.5px) | 12px 이하 금지 — 미달 |
| `.auth-hint` 8자 이상 | 0.75rem (12px) | 12px 이하 금지 — 미달 |
| `.auth-switch` 전환 링크 | 0.82rem (13px) | 이름표 바닥 13px — 통과 |

가장 조심해서 쓴 문구인 개인정보 3줄이 화면에서 가장 작다.

### 1.3 터치 타깃이 9월 기준 아래다

`.password-toggle-button` 은 아이콘 20px + 패딩 6px = 32px 다. 2026-09-11 에 팀이 같은 파일에서 다른 버튼을 44px 로 올리며 이렇게 적었다 — "WCAG 2.5.8(24×24)은 넘어도 손가락으로는 빠듯했다." 비밀번호 눈 버튼은 그 기준을 아직 안 받았다.

### 1.4 데스크톱이 비어 있다

`.signin-shell` 은 `--blue-50` 단색 위에 480px 칼럼 하나다. 1440px 화면이면 좌우로 각각 480px 가 빈다. 이건 결함이자 기회다 — 요구사항 2번("어떤 서비스인지")이 들어갈 자리다.

### 1.5 첫 실행 화면의 고지가 사실과 다르다 — 가장 무겁다

ADR-011(승인, 2026-09-04)이 건강정보 정본을 PostgreSQL 로 옮겼다. 로그인 화면의 리드 문구는 그때 고쳤고, 코드 주석이 이유를 남겼다.

> 동의 직전 화면에서 틀린 약속을 하는 것이 이 축에서 가장 무거운 결함이었다.

**첫 실행 화면은 안 고쳐졌다.** 네 군데가 ADR-011 이전 문구로 남아 있다.

| 위치 | 지금 문구 | 실제 |
|---|---|---|
| `HomePage.tsx:1084` | "별도 로그인 없이 … **로컬 프로필**입니다" | 서버 저장 |
| `HomePage.tsx:659` | 꼬리표 "이 기기에 저장" · 제목 "가족 구성원 **로컬** 프로필 만들기" | 서버 저장 |
| `HomePage.tsx:661` | "이 브라우저에 암호화해 저장하며 **서버로 보내지 않습니다**" | 서버로 보낸다 |
| `HomePage.tsx:1092` | 3단계 "백업 파일 보관 / 암호화해 직접 내보내기" | ADR-007 대체됨 |

세 번째가 특히 나쁘다. 사용자가 이름·관계·성별·생년을 **입력하는 그 화면에서** 서버로 안 보낸다고 적어 놓고 보낸다. 경로는 이렇다.

```
HomePage.submitProfile
  → useLocalDomain().createProfile
  → LocalDomainProvider.tsx:123   createServerDomainRuntime(...)
  → serverDomainRuntime.ts:112    client.createProfile({ … })
  → POST /api/v1/profiles
```

`LocalDomainProvider` 가 로컬 런타임을 타는 것은 `databaseName` 이 들어오고 그 값이 `"ieobom-local"` 이 아닐 때뿐이다. 단위 테스트 전용 통로다. 로그인한 실제 사용자는 언제나 서버 런타임을 탄다.

### 1.6 가입 직후에 서비스 설명이 없다

가입 → `/` → `EmptyHousehold`. 거기 3단계 목록이 있지만 **서비스가 무엇을 하는지가 아니라 사용자가 할 일**을 적는다. 그중 3번은 폐기된 기능이다.

```
1  프로필 만들기      이름·관계·생년 정보
2  건강기록 남기기    검진·통증·건강 메모
3  백업 파일 보관     암호화해 직접 내보내기    ← ADR-007 대체
```

방금 가입한 사람은 이 서비스가 검진표를 읽고 만성질환을 판정한다는 것을 아직 모른다.

---

## 2. 설계 원칙

1. **토큰을 새로 만들지 않는다.** 쓰는 값은 전부 styles.css 에 있다. 예외는 §3.2 의 `--auth-*` 사다리 하나이고, 값은 기존 `--as-*` 와 같다.
2. **사실이 아닌 문장을 화면에 두지 않는다.** 저장 위치·열람 범위·원본 보관은 ADR-011 에 맞춘다. 확인할 수 없으면 쓰지 않는다.
3. **숫자는 상수에서 끌어온다.** "만성질환 14가지" 를 손으로 적지 않는다. `DISEASE_NAMES` 에서 센다. 질환이 늘면 카피가 따라 는다.
4. **접근 가능한 이름을 깨지 않는다.** 테스트가 잡는 제목·버튼 이름은 그대로 둔다(§9.2).
5. **폼을 두 벌 만들지 않는다.** 첫 실행 화면과 구성원 추가 모달은 같은 필드를 쓴다 — 컴포넌트 하나로 뺀다.

---

## 3. 토큰

### 3.1 그대로 쓰는 것

```
바탕    --blue-50       #f4f7ff    관문 배경
카드    --surface       #ffffff    인증 카드
        --line          #dce3ee    카드 테두리
        --card-shadow   0 10px 32px rgba(35,61,112,.05)
글자    --ink           #172033    제목·입력값
        --muted-strong  #45536c    본문
        --muted         #5b687e    보조 (2026-09-10 실측 최저 4.79)
액센트  --blue-600      #2463eb    링크·기본 버튼 테두리
        --blue-700      #1d4fb8    아이콘 stroke
        --blue-100      #eaf1ff    아이콘 타일 배경
상태    --danger #b43e47 / --danger-soft #fff0f1
        --safe-text #0f6b50 / --safe-soft #e7f7f1
반경    버튼·입력칸 12px · 카드 20px (`.account-card`)
```

`.account-card` 의 반경 20px 은 시스템 카드 22px(`--card-radius`)과 2px 다르다. **그대로 둔다.** 계정 화면도 이 클래스를 쓰고, 2px 때문에 두 화면을 건드릴 값어치가 없다.

### 3.2 새로 더하는 것 — 인증 화면 글자 사다리

```css
/* 인증 화면(관문·가입·첫 실행) 글자 사다리.
 *
 * 값은 판정 화면 사다리(`--as-*`)와 같다. 근거도 같다 — 주 사용자가 30~60 대라
 * 문장의 바닥은 15px, 이름표의 바닥은 13px 이고 12px 이하는 두지 않는다.
 *
 * 그런데 `--as-*` 를 그대로 쓰지 않고 이름을 따로 둔다. 그 토큰은 스스로
 * "판정 화면 전용이다 — 다른 화면의 크기를 건드리지 않는다" 고 범위를 못박았다.
 * 별칭으로 묶으면 판정 화면 사다리를 손볼 때 로그인 화면이 같이 끌려간다.
 * 값이 같은 것은 지금 근거가 같아서지, 두 화면이 한 몸이어서가 아니다. */
--auth-title: 1.75rem;   /* 28px · 화면 제목(h1) */
--auth-h2:    1.25rem;   /* 20px · 카드 제목 */
--auth-body:  1rem;      /* 16px · 본문·입력칸 */
--auth-sub:   0.9375rem; /* 15px · 보조 문장 — 문장의 바닥 */
--auth-label: 0.8125rem; /* 13px · 꼬리표·힌트 — 이름표의 바닥 */
```

적용은 이렇게 바뀐다.

| 자리 | 지금 | 바꾼 뒤 |
|---|---|---|
| `.signin-panel h1` | 1.5rem (24px) | `--auth-title` 1.75rem |
| `.signin-lead` | 0.88rem (14px) | `--auth-sub` 0.9375rem |
| `.signin-notes` | 0.78rem (12.5px) | `--auth-sub` 0.9375rem |
| `.signin-brand small` | 0.78rem (12.5px) | `--auth-label` 0.8125rem |
| `.auth-hint` | 0.75rem (12px) | `--auth-label` 0.8125rem |
| `.auth-switch` | 0.82rem (13px) | `--auth-label` 0.8125rem (거의 동일) |

### 3.3 반응 분기

**1000px 하나만 쓴다.** styles.css 에 이미 있는 분기라 새 값을 더하지 않는다(현재 쓰이는 값: 320 · 440 · 450 · 480 · 520 · 560 · 640 · 720 · 760 · 1000 · 1024 · 1080).

```
≥ 1000px    2칼럼 — 왼쪽 소개 · 오른쪽 인증 카드
<  1000px   1칼럼 — 지금과 같은 480px 가운데 정렬, 소개는 카드 아래
```

---

## 4. 화면 1 — 로그인 관문

### 4.1 배치 (≥1000px)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│   ┌────┐                               ┌───────────────────────────┐    │
│   │icon│  이어봄                        │ 서비스 계정                │    │
│   │56px│  우리 가족 건강기록             │ 로그인                     │    │
│   └────┘                               │                           │    │
│                                        │ 이메일                     │    │
│   검진 결과를                           │ ┌───────────────────────┐ │    │
│   가족 단위로 이어 봅니다.               │ └───────────────────────┘ │    │
│                                        │ 비밀번호                   │    │
│   ┌──┐  검진표를 사진으로 올리면         │ ┌─────────────────┬─────┐ │    │
│   │▤ │  수치를 읽어 옵니다               │ └─────────────────┴ ◉ ─┘ │    │
│   └──┘                                 │                           │    │
│   ┌──┐  검진 수치 한 벌로 만성질환       │ ┌───────────────────────┐ │    │
│   │▥ │  14가지를 한 번에 판정합니다      │ │        로그인          │ │    │
│   └──┘                                 │ └───────────────────────┘ │    │
│   ┌──┐  가족 구성원마다 따로 기록하고    │                           │    │
│   │▦ │  함께 봅니다                     │ 아직 계정이 없으신가요?    │    │
│   └──┘                                 │ 회원가입                   │    │
│                                        │ 비밀번호를 잊으셨나요?     │    │
│                                        │ 비밀번호 찾기              │    │
│                                        └───────────────────────────┘    │
│                                                                         │
│                                        · 건강기록·프로필·판정 결과는    │
│                                          로그인한 계정에 저장됩니다.     │
│                                        · 검진표 원본은 보관하지 않고,   │
│                                          읽어 들이는 동안에만 씁니다.    │
│                                        · 같은 가정 구성원 외에는        │
│                                          열람할 수 없습니다.            │
└─────────────────────────────────────────────────────────────────────────┘
      ← 소개 (최대 420px)              → 인증 (480px 고정)
```

개인정보 3줄은 **카드 아래에 둔다.** 왼쪽 소개 칼럼으로 옮기면 더 크게 보이지만, 이 세 줄은 제출 버튼과 같은 시선 흐름 안에 있어야 한다 — 가입 전에 하는 약속이다.

### 4.2 DOM 순서와 시각 순서를 갈라 둔다

브랜드·인증·소개 **셋을 형제로 둔다.** 자리는 `grid-template-areas` 가 정한다.

```html
<div class="auth-shell">
  <div class="signin-brand">…</div>   <!-- area: brand -->

  <div class="auth-main">             <!-- area: main -->
    <h1>로그인하고 시작하세요</h1>
    <p class="signin-lead">…</p>
    <!-- alert / status -->
    <AuthCard />                      <!-- h2 로그인 -->
    <ul class="signin-notes">…</ul>
  </div>

  <aside class="auth-intro" aria-label="이어봄이 하는 일">  <!-- area: intro -->
    <p class="auth-pitch">검진 결과를 가족 단위로 이어 봅니다.</p>
    <ul class="auth-points">…</ul>
  </aside>
</div>
```

```css
.auth-shell {
  min-height: 100vh;
  display: grid;
  grid-template-columns: min(480px, 100%);
  grid-template-areas: "brand" "main" "intro";
  align-content: center;
  justify-content: center;
  gap: 20px;
  padding: 32px 20px;
  background: var(--blue-50);
}
.auth-shell .signin-brand { grid-area: brand; }
.auth-main { grid-area: main; }
.auth-intro { grid-area: intro; }

@media (min-width: 1000px) {
  .auth-shell {
    grid-template-columns: minmax(0, 420px) 480px;
    grid-template-areas:
      "brand main"
      "intro main";
    column-gap: 64px;
    row-gap: 24px;
  }
  .auth-intro { justify-content: center; }
  .signin-brand { align-self: end; }
}
```

```
  넓을 때          좁을 때
  brand  main      brand
  intro  main      main
                   intro
```

이렇게 가른 이유가 셋이다.

- **제목 순서.** 소개를 DOM 에서 먼저 두면 `h2`(소개) → `h1`(관문) 순서가 되어 제목 단계가 거꾸로 선다. 인증 블록을 먼저 두면 `h1` → `h2` 로 바로 선다. 소개는 제목을 갖지 않고 `aria-label` 만 단다.
- **모바일 순서가 공짜로 나온다.** 1칼럼으로 접히면 자연 순서가 폼 먼저, 소개 나중이다. 로그인하러 온 사람에게 소개를 먼저 스크롤시키지 않는다.
- **브랜드는 어느 폭에서나 맨 위다.** 관문에서 브랜드를 한 번 보여 준다는 원래 의도(styles.css 주석)가 1칼럼에서도 지켜져야 한다.

> **`order` 로는 안 된다.** 처음에 이 문서는 브랜드를 소개 칼럼 안에 넣고 `order: -1` 로 끌어올리는 안을 적었는데, 구현해 보니 틀렸다 — `order` 는 **같은 플렉스 컨테이너의 형제끼리만** 순서를 바꾼다. 소개 안에 든 브랜드는 아무리 `order` 를 줘도 소개 칼럼 밖으로 못 나온다. 셋을 형제로 올리고 자리를 이름으로 지정해야 풀린다.

### 4.3 소개 항목

```html
<ul class="auth-points">
  <li>
    <span class="auth-point-icon" aria-hidden="true"><!-- inline svg --></span>
    <span>검진표를 사진으로 올리면 수치를 읽어 옵니다.</span>
  </li>
  …
</ul>
```

```css
.auth-points { margin: 0; padding: 0; list-style: none; display: grid; gap: 18px; }
.auth-points li { display: flex; gap: 14px; align-items: center;
                  font-size: var(--auth-sub); color: var(--muted-strong); line-height: 1.6; }
.auth-point-icon {
  width: 40px; height: 40px; flex-shrink: 0;
  display: inline-flex; align-items: center; justify-content: center;
  border-radius: 12px;
  background: var(--blue-100);
  color: var(--blue-700);
}
.auth-pitch { margin: 0; font-size: var(--auth-title); line-height: 1.45; color: var(--ink); font-weight: 800; }
```

아이콘은 **인라인 SVG** 다. 이모지를 쓰지 않는다(위 도식의 `▤▥▦` 는 자리를 보이려고 그린 것이다). 규격은 `AuthCard` 의 눈 아이콘과 맞춘다 — `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `stroke-width="2"`, 둥근 캡, `aria-hidden="true"`.

| 항목 | 아이콘 | 문장 |
|---|---|---|
| 1 | 문서 + 카메라 | 검진표를 사진으로 올리면 수치를 읽어 옵니다. |
| 2 | 막대 그래프 | 검진 수치 한 벌로 만성질환 {n}가지를 한 번에 판정합니다. |
| 3 | 사람 둘 | 가족 구성원마다 따로 기록하고 함께 봅니다. |

`{n}` 은 상수에서 센다. 손으로 적지 않는다.

```ts
import { DISEASE_NAMES } from "../assessment/contracts";
const DISEASE_COUNT = Object.keys(DISEASE_NAMES).length;   // 지금은 14
```

`contracts.ts` 는 서버 `SPECS` 의 손 사본이고 "순서·표기를 맞춘다" 고 스스로 적어 뒀다. 서버가 질환을 더하면 이 숫자가 따라 는다. 상수로 박아 두면 안 따라온다.

### 4.4 브랜드

```css
.signin-brand img    { width: 42px; height: 42px; }
.signin-brand strong { font-size: 1.125rem; }              /* 18px */
.signin-brand small  { font-size: var(--auth-label); }     /* 12.5 → 13px */

@media (min-width: 1000px) {
  .signin-brand img { width: 56px; height: 56px; }
}
```

`.brand-mark` 의 반경 14px 과 그림자 `0 10px 24px rgba(36,99,235,.24)` 는 그대로 쓴다.

### 4.5 카드에서 덜어내는 것

카드 안의 설명 문단을 모드별로 다르게 처리한다.

| 모드 | 지금 카드 `<p>` | 바꾼 뒤 |
|---|---|---|
| `signin` | "가입하신 이메일로 로그인하세요." | **삭제** — kicker + h2 로 충분하고 위 리드와 겹친다 |
| `signup` | "계정 하나로 건강기록·가족 연결·구독을 함께 관리합니다." | 유지 |
| 초대받음 | "{email} 주소로 초대받았습니다…" | 유지 — 테스트가 잡는다 |
| `forgot-password` | "가입하신 이메일 주소를 입력하시면…" | 유지 |
| `reset-password` | "새로 사용할 비밀번호를 8자 이상 입력하세요." | 유지 |

`signin` 하나만 지운다. 나머지는 위 리드와 말이 겹치지 않는다.

### 4.6 비밀번호 눈 버튼 — 44px

```css
.password-toggle-button {
  /* 2026-09-11 에 같은 파일이 다른 버튼을 44px 로 올린 기준을 여기도 받는다.
     WCAG 2.5.8(24×24)은 32px 로도 넘지만 손가락으로는 빠듯했다. */
  min-width: 44px;
  min-height: 44px;
  right: 4px;
  padding: 0;
}
.password-input-wrapper input { padding-right: 52px; }
```

아이콘 자체는 20px 그대로다. 커지는 것은 눌리는 면적이다.

---

## 5. 화면 2 — 회원가입 (`/signup`)

관문과 **같은 껍데기**를 쓴다. `auth-shell` · `auth-intro` · `auth-points` 를 그대로 두고 오른쪽 내용만 바꾼다. 두 화면이 다르게 생기면 전환 링크를 눌렀을 때 다른 서비스로 넘어온 것처럼 보인다.

| 자리 | 관문 | 가입 |
|---|---|---|
| `h1` | 로그인하고 시작하세요 | 이어봄 시작하기 |
| 리드 | 질환 예측과 검진표 인식은 서비스 계정이… | 이메일과 비밀번호만 있으면 됩니다… |
| 카드 kicker | 서비스 계정 | 서비스 계정 |
| 카드 `h2` | 로그인 | 서비스 계정 만들기 |
| 카드 아래 | 회원가입 · 비밀번호 찾기 전환 | 이미 계정이 있으신가요? 로그인 |
| 소개 칼럼 | 동일 | 동일 |
| 개인정보 3줄 | 동일 | 동일 |

`h1` 둘과 카드 `h2` 둘은 **테스트가 이름으로 잡는다.** 바꾸지 않는다(§9.2).

소개 칼럼을 가입에서도 그대로 두는 것이 요구사항 2번의 절반이다 — 계정을 만드는 바로 그 화면에서 무엇을 하는 서비스인지 읽힌다.

---

## 6. 화면 3 — 첫 실행 (계정 초기정보 세팅)

가입에 성공하면 `SignUpPage` 가 원래 가려던 주소로 넘긴다. 넘어온 주소가 없으면 `/` 이고, 거기서 프로필이 0개면 `EmptyHousehold` 가 그려진다. **이 화면이 곧 온보딩이다.**

### 6.1 새 라우트를 만들지 않는다

`/welcome` 같은 주소를 따로 파는 안을 검토했고 기각했다. 이유 셋이다.

- 관문 밖에 둘 수 있는 화면은 `/signup` 하나라고 `router.test.tsx` 가 못박았다. 온보딩은 로그인 뒤라 관문 안이어야 하고, 그러면 헤더·내비가 이미 붙는다.
- "프로필이 있으면 비킨다" 는 분기를 한 벌 더 써야 한다. `SignUpPage` 가 쓰는 것과 같은 판단을 두 곳에 두게 된다(`AGENTS.md` 5번).
- 지금도 첫 화면은 `EmptyHousehold` 다. 자리를 옮길 게 아니라 그 자리를 고치면 된다.

### 6.2 배치 (≥1000px)

```
┌──────────────────────────────────────────────────────────────────────────┐
│  첫 단계                                                                  │
│  이어봄에 오신 것을 환영합니다                                             │
│  건강기록을 남길 첫 구성원을 만들어 주세요. 나중에 더 추가할 수 있습니다.   │
│                                                                          │
│  ┌─ 첫 구성원 만들기 ──────────────┐   ┌─ 이어봄이 하는 일 ─────────────┐ │
│  │ 이름 또는 호칭                   │   │ ┌──┐ 검진표를 사진으로 올리면  │ │
│  │ ┌────────────────────────────┐  │   │ │▤ │ 수치를 읽어 옵니다        │ │
│  │ │ 예: 나, 엄마, 민준          │  │   │ └──┘                          │ │
│  │ └────────────────────────────┘  │   │ ┌──┐ 검진 수치 한 벌로         │ │
│  │ 관계                             │   │ │▥ │ 만성질환 14가지를        │ │
│  │ ┌────────────────────────────┐  │   │ └──┘ 한 번에 판정합니다        │ │
│  │ │ 관계를 선택하세요         ▾ │  │   │ ┌──┐ 가족 구성원마다 따로     │ │
│  │ └────────────────────────────┘  │   │ │▦ │ 기록하고 함께 봅니다      │ │
│  │ 성별                             │   │ └──┘                          │ │
│  │ ┌────────────────────────────┐  │   └───────────────────────────────┘ │
│  │ │ 남성 또는 여성            ▾ │  │                                     │
│  │ └────────────────────────────┘  │                                     │
│  │ 생년월일                         │                                     │
│  │ ┌────────────────────────────┐  │                                     │
│  │ │ 1990-01-01                 │  │                                     │
│  │ └────────────────────────────┘  │                                     │
│  │ ┌────────────────────────────┐  │                                     │
│  │ │         프로필 저장         │  │                                     │
│  │ └────────────────────────────┘  │                                     │
│  └─────────────────────────────────┘                                     │
│                                                                          │
│  건강기록·프로필·판정 결과는 로그인한 계정에 저장되고, 같은 가정 구성원만  │
│  열람할 수 있습니다.                                                      │
└──────────────────────────────────────────────────────────────────────────┘
```

`< 1000px` 에서는 한 칼럼으로 쌓인다. 순서는 제목 → 폼 → 소개 → 고지다. 여기서는 폼이 소개보다 위다 — 가입을 막 끝낸 사람이라 서비스 설명은 이미 두 화면에서 봤고, 지금 할 일은 입력이다.

### 6.3 모달을 걷어내고 인라인 폼으로

지금은 "첫 구성원 등록" 버튼 → 모달이다. 첫 실행에서는 **버튼 없이 폼을 바로 편다.** 클릭 하나가 줄고, 모달의 좁은 폭에 눌려 있던 필드 넷이 제 폭을 쓴다.

구성원을 **나중에 더하는** 경로(`HomePage.tsx:536` 의 "우리 가정에 새 프로필 만들기")는 모달 그대로 둔다. 목록을 보다가 하나 더하는 흐름이라 화면을 갈아 끼울 이유가 없다.

그러면 같은 필드가 두 곳에 생긴다. **필드를 컴포넌트로 뺀다.**

```
features/home/ProfileFields.tsx     이름·관계·성별·생년월일 네 칸만 그린다
  ├─ EmptyHousehold 의 인라인 폼이 쓴다
  └─ 구성원 추가 모달이 쓴다
```

제출 버튼과 취소 버튼은 부르는 쪽이 넘긴다 — 모달은 취소가 있고 첫 실행은 없다. `AuthCard` 가 `footer` 를 호출부에서 받는 것과 같은 이유다.

### 6.4 카피를 사실로 되돌린다

§1.5 의 네 자리를 전부 고친다.

| 위치 | 지금 | 바꾼 뒤 |
|---|---|---|
| 모달 꼬리표 | 이 기기에 저장 | 우리 가정 |
| 모달 제목 | 가족 구성원 로컬 프로필 만들기 | 가족 구성원 프로필 만들기 |
| 모달 고지 | 이 브라우저에 암호화해 저장하며 서버로 보내지 않습니다. | 입력한 정보는 로그인한 계정에 저장되고, 같은 가정 구성원만 열람할 수 있습니다. |
| 첫 실행 설명 | 별도 로그인 없이 건강기록의 대상을 구분하는 로컬 프로필입니다. | 건강기록을 남길 대상입니다. 가족 구성원마다 하나씩 만듭니다. |
| 3단계 목록 | 프로필 → 건강기록 → 백업 파일 | **삭제.** 자리에 "이어봄이 하는 일" 3점이 들어간다 |

"백업 파일 보관 / 암호화해 직접 내보내기" 는 ADR-007 이 대체됐으므로 지운다. 내보내기 기능 자체는 `/data` 에 남아 있지만 **첫 단계로 안내할 일이 아니다** — 방금 가입한 사람에게 세 번째로 시킬 일이 백업일 수는 없다.

### 6.5 소개 3점을 세 화면에서 공유한다

관문·가입·첫 실행이 같은 세 문장을 쓴다. 한 곳에 둔다.

```
features/account/servicePoints.tsx
  export const SERVICE_POINTS = [ {icon, text}, … ]
```

세 화면이 각자 문장을 들고 있으면 하나만 고쳐지고 나머지는 남는다. §1.5 가 정확히 그렇게 생긴 결함이다 — 관문은 고쳐졌고 첫 실행은 안 고쳐졌다.

---

## 7. 카피 원문

고치는 것만 적는다. 표에 없는 문구는 지금 그대로다.

### 7.1 공통 — 서비스 소개 3점

```
검진표를 사진으로 올리면 수치를 읽어 옵니다.
검진 수치 한 벌로 만성질환 {n}가지를 한 번에 판정합니다.
가족 구성원마다 따로 기록하고 함께 봅니다.
```

### 7.2 공통 — 개인정보 3줄 (관문·가입, 지금 문구 유지)

```
건강기록·프로필·판정 결과는 로그인한 계정에 저장됩니다.
검진표 원본은 보관하지 않고, 읽어 들이는 동안에만 씁니다.
같은 가정 구성원 외에는 열람할 수 없습니다.
```

ADR-011 에 맞춰 이미 고쳐진 문장이다. **한 글자도 바꾸지 않는다.**

### 7.3 관문

| 자리 | 문구 |
|---|---|
| 소개 큰 문장 (신규) | 검진 결과를 가족 단위로 이어 봅니다. |
| `h1` | 로그인하고 시작하세요 *(유지 — 테스트)* |
| 리드 | 질환 예측과 검진표 인식은 서비스 계정이 있어야 씁니다. 건강기록은 계정에 저장되어 기기를 바꿔도 이어집니다. *(유지)* |
| 카드 `p` | *(삭제)* |

### 7.4 첫 실행

| 자리 | 문구 |
|---|---|
| 꼬리표 | 첫 단계 *(유지)* |
| 제목 | 이어봄에 오신 것을 환영합니다 |
| 설명 | 건강기록을 남길 첫 구성원을 만들어 주세요. 나중에 더 추가할 수 있습니다. |
| 폼 제목 | 첫 구성원 만들기 |
| 소개 제목 | 이어봄이 하는 일 |
| 제출 버튼 | 프로필 저장 / 저장 중… *(유지)* |
| 하단 고지 | 건강기록·프로필·판정 결과는 로그인한 계정에 저장되고, 같은 가정 구성원만 열람할 수 있습니다. |

---

## 8. 상태 다섯 가지

세 화면 모두 이미 있는 장치를 쓴다. 새로 만드는 것은 없다.

| 상태 | 관문·가입 | 첫 실행 |
|---|---|---|
| **비어 있음** | 해당 없음 | 화면 자체가 빈 상태 |
| **불러오는 중** | `status === "checking"` → `.route-loading` "불러오는 중…" | `.dashboard-skeleton` |
| **처리 중** | 버튼 `disabled` + "처리 중…" / "발송 중…" / "변경 중…" | 버튼 `disabled` + "저장 중…" |
| **오류** | `.alert.error-alert` `role="alert"` | 같음 |
| **성공** | `.alert.success-alert` `role="status"` | 저장되면 대시보드로 바뀐다 (알림 없음) |

세션 확인 중에는 아무것도 그리지 않는다. `SignUpPage` 주석이 이유를 적어 뒀다 — 이미 로그인한 사람에게 가입 폼이 한 번 깜빡이면 계정이 없어진 것처럼 보인다.

---

## 9. 접근성

### 9.1 지킬 것

| 항목 | 기준 | 어떻게 |
|---|---|---|
| 제목 단계 | `h1` → `h2`, 건너뛰지 않는다 | DOM 순서를 인증 먼저로(§4.2) |
| 본문 대비 | WCAG 1.4.3 — 4.5:1 | `--muted` 는 `--blue-50` 위 실측 통과. 새 조합은 §9.3 에서 잰다 |
| 타깃 크기 | 44×44 (팀 기준, 2026-09-11) | 눈 버튼 32 → 44px |
| 장식 요소 | 이름 없음 | 아이콘 `aria-hidden="true"`, 소개 아이콘도 포함 |
| 포커스 | 전역 규칙 | `outline: 3px solid rgba(36,99,235,.2)` 그대로 |
| 폼 이름 | 라벨이 입력을 감싼다 | 지금 구조 유지 |
| 알림 | `role="alert"` / `role="status"` | 지금 구조 유지 |
| 모션 | `prefers-reduced-motion` | 새 전환을 넣지 않으므로 해당 없음 |

소개 목록은 `<ul>` 이다. 아이콘이 `aria-hidden` 이라 읽히는 것은 문장뿐이다.

### 9.2 깨면 안 되는 접근 가능한 이름

테스트가 이 이름들로 요소를 찾는다. 카피를 손볼 때 이 표를 먼저 본다.

| 이름 | 역할 | 잡는 곳 |
|---|---|---|
| 로그인하고 시작하세요 | heading level 1 | `SignInPage.test.tsx:89, 155` |
| 이어봄 시작하기 | heading level 1 | `SignInPage.test.tsx:68` |
| 비밀번호 찾기 | heading level 1 | `SignInPage.test.tsx:129` |
| 새 비밀번호 설정 | heading level 1 | `SignInPage.test.tsx:147` |
| 회원가입 / 로그인 | button | `SignInPage.test.tsx:67, 87, 100` |
| 비밀번호 찾기 / 재설정 링크 받기 | button | `SignInPage.test.tsx:128, 132` |
| 비밀번호 보기 / 비밀번호 숨기기 | button | `SignInPage.test.tsx:189, 193` |
| 로그인 | link | `SignUpPage.test.tsx:104` |
| `{email} 주소로 초대받았습니다` | text | `SignInPage.test.tsx:118` |

`h1` 을 `--auth-title` 로 키우는 것은 이름을 바꾸지 않는다. 카드 `<p>` 삭제(§4.5)도 이 표에 없다.

### 9.3 실측한 대비 (2026-09-11)

새로 생기는 조합을 쟀다. 전부 통과다.

| 앞 | 뒤 | 실측 | 기준 | 어디 |
|---|---|---|---|---|
| `--ink` #172033 | `--blue-50` #f4f7ff | **15.18** | 4.5 | 소개 큰 문장 |
| `--muted-strong` #45536c | `--blue-50` #f4f7ff | **7.24** | 4.5 | 소개 3점 · 리드 |
| `--muted` #5b687e | `--blue-50` #f4f7ff | **5.26** | 4.5 | 개인정보 3줄 |
| `--muted` #5b687e | `--surface` #ffffff | **5.64** | 4.5 | 카드 안 보조 |
| `--blue-600` #2463eb | `--surface` #ffffff | **5.17** | 4.5 | 전환 링크 |
| `--blue-600` #2463eb | `--blue-50` #f4f7ff | **4.83** | 4.5 | 전환 링크(관문 바탕) |
| `--blue-700` #1d4fb8 | `--blue-100` #eaf1ff | **6.47** | 3.0 (1.4.11) | 아이콘 stroke — 글자가 아니다 |

개인정보 3줄이 5.26 으로 여유가 있다. 글자를 12.5px 에서 15px 로 올린 것과 별개로 색도 통과한다.

---

## 10. 반응형

| 폭 | 관문·가입 | 첫 실행 |
|---|---|---|
| ≥ 1000px | 2칼럼 — 소개 420px + 인증 480px, 간격 64px | 2칼럼 — 폼 + 소개 |
| 760~999px | 1칼럼 480px. 브랜드 → 인증 → 소개 → 고지 | 1칼럼. 제목 → 폼 → 소개 → 고지 |
| < 760px | 같음. 좌우 여백 20px | 같음 |
| < 320px | 다루지 않는다 — `html { min-width: 320px }` | 같음 |

소개 칼럼은 1칼럼에서도 **숨기지 않는다.** 모바일에서 처음 오는 사람이 더 많고, 그 사람에게 필요한 것이 그 세 줄이다. 스크롤 아래로 내려갈 뿐이다.

---

## 11. 구현 순서 — 2026-09-11 완료

사실관계 오류부터 고친다. 화면이 예뻐지는 것보다 먼저다.

아래 표대로 구현했다. 계획과 달라진 세 가지는 §11.1 에 적는다.

| 순서 | 무엇 | 파일 |
|---|---|---|
| 1 | §1.5 의 네 자리 카피를 사실로 되돌린다 | `features/home/HomePage.tsx` |
| 2 | 소개 3점 상수를 만든다 | `features/account/servicePoints.tsx` (신규) |
| 3 | `--auth-*` 사다리를 `:root` 에 더한다 | `styles.css` |
| 4 | `auth-shell` 2칼럼과 소개 칼럼 | `styles.css` |
| 5 | 관문·가입을 새 껍데기로 옮긴다 | `SignInPage.tsx` · `SignUpPage.tsx` |
| 6 | 카드 `signin` 모드의 `<p>` 를 지운다 | `AuthCard.tsx` |
| 7 | 눈 버튼 44px | `styles.css` |
| 8 | `ProfileFields` 를 빼고 첫 실행을 인라인 폼으로 | `features/home/ProfileFields.tsx` (신규) · `HomePage.tsx` |

### 검증

`AGENTS.md` 가 정한 순서 그대로다.

```bash
cd frontend && npm run lint && npm run typecheck && npm test && npm run build
```

프런트만 건드리므로 파이썬 쪽은 돌릴 것이 없다. 스택에 반영하려면 프런트를 다시 빌드해야 한다.

```bash
docker compose build fastapi && docker compose up -d fastapi
```

### 테스트가 따라 바뀌는 곳

| 테스트 | 예상 | 실제 |
|---|---|---|
| `SignInPage.test.tsx` | 카드 `<p>` 삭제로 손볼 수 있다 | **손댈 것 없었다.** 잡는 이름이 전부 유지됐다 |
| `SignUpPage.test.tsx` | 손댈 것 없음 | 그대로 통과 |
| `HomePage.test.tsx` | 모달 경유 → 인라인 폼 | 네 줄 고쳤다 — `"첫 구성원 등록"` 버튼 클릭을 `"첫 구성원 만들기"` 제목 대기로 바꿨다(22·55·74·368행) |

### 11.1 계획과 달라진 것

1. **`order` 대신 `grid-template-areas`.** §4.2 의 첫 안이 동작하지 않았다. 이유는 그 절의 인용 상자에 적었다.
2. **고칠 자리가 넷이 아니라 다섯이었다.** §1.5 표를 만들 때 프로필 **수정** 모달의 `form-notice`(`HomePage.tsx` 916행, "프로필 정보와 건강기록은 계속 이 브라우저에만 저장됩니다")를 놓쳤다. 생성 모달과 같은 거짓말이고 같은 폼이라 같이 고쳤다.
3. **`ProfileFields` 를 쓰는 곳이 둘이 아니라 셋.** 첫 실행·생성 모달에 더해 **수정 모달**도 같은 네 칸이었다. 셋이 한 컴포넌트를 쓴다. 관계 보기(`RELATIONSHIPS`)도 `HomePage.tsx` 에서 그리로 옮겼다.

부수로 죽은 CSS 하나를 지웠다 — `.empty-household` 를 좁은 화면에서 1칼럼으로 되돌리던 미디어 쿼리 규칙. 후신인 `.first-run-body` 는 기본이 1칼럼이라 되돌릴 것이 없다.

### 11.2 검증 결과

| 단계 | 결과 |
|---|---|
| `npm run lint` | 오류 0 · 경고 3(전부 손대지 않은 파일의 기존 것) |
| `npm run typecheck` | 통과 |
| `npm test` | **458 통과 / 5 실패** |
| `npm run build` | 통과 |

남은 5 실패는 **이 작업 전부터 있던 것이고 인증·온보딩과 무관하다.** 세 파일 모두 여기서 바꾼 모듈(`HomePage`·`servicePoints`·`ProfileFields`·`AuthCard`·`SignInPage`·`SignUpPage`)을 한 번도 참조하지 않는다.

| 파일 | 실패 | 무엇에 관한 것인가 |
|---|---|---|
| `AssessmentPage.test.tsx` | 1 | 매트릭스 축 · 궤적 섹션 제목 |
| `DocumentPane.test.tsx` | 1 | OCR "확인이 필요한 1개" |
| `FamilyIntegratedMonitoring.test.tsx` | 3 | 간암 장기 배지 · 가족력 구분 |

---

## 12. 하지 않는 것

- **토큰 값을 바꾸지 않는다.** `--blue-*`·`--card-*`·`--muted` 는 그대로다. 새로 더하는 것은 `--auth-*` 다섯뿐이다.
- **상위 `Team_project/docs/DESIGN.md` 로 맞추지 않는다.** 2026-09-11 에 현재 구현을 유지하기로 했다. 문서 쪽으로 정렬하면 9월 대비 작업을 전부 재검증해야 한다.
- **`.account-card` 반경을 건드리지 않는다.** 계정 화면과 공유한다(§3.1).
- **새 라우트를 만들지 않는다.** `/welcome` 기각 이유는 §6.1.
- **구성원 추가 모달을 없애지 않는다.** 첫 실행만 인라인이다(§6.3).
- **애니메이션을 넣지 않는다.** 지금 인증 화면에 전환이 없고, 넣으면 `prefers-reduced-motion` 분기가 따라붙는다. 이 작업에서 벌 일이 아니다.

## 13. 열린 것 — 이 문서 범위 밖에서 확인할 것

작업 중에 본 것인데 세 화면 밖이라 손대지 않았다.

### 13.1 ADR-011 이전 문구가 아홉 곳 남아 있다

§1.5 의 자리들이 전부가 아니었다. 전수로 세니 프로필 밖에도 같은 결함이 있었고, 그중 프로필 관련 다섯(생성 모달 꼬리표·제목·고지, 수정 모달 꼬리표·고지)은 이 작업에서 고쳤다. **아홉 곳이 남는다.**

| 파일 | 남은 곳 | 무엇 |
|---|---|---|
| `HomePage.tsx` | 5 | 모달 꼬리표 `kicker="이 기기에 저장"` — 723 · 823 · 854 · 869 · 976 (건강기록 작성·수정·삭제·복원, 숨긴 프로필) |
| `AccountPage.tsx` | 3 | "로컬 건강정보는 삭제되지 않습니다" 340 · 850 · "기기에 저장된 건강정보는 삭제되지 않습니다" 478 |
| `app/services/prediction.py` | 1 | 고지 문구 "입력한 값은 저장하지 않습니다." 41 |

꼬리표는 장식이 아니다. `Modal.tsx:100` 이 `.section-kicker` 로 그린다. 건강기록을 쓰고 고치고 지우는 모달이 아직 "이 기기에 저장" 을 달고 있는데 전부 서버로 간다.

마지막 줄이 특히 눈에 걸린다. ADR-011 §1 은 예측 결과를 PostgreSQL 저장 대상으로 못박아 뒀는데, 판정 화면은 서버가 보낸 고지로 저장하지 않는다고 말한다. 프런트가 아니라 **서버에서** 고쳐야 한다.

아홉을 여기서 같이 고치지 않은 것은 범위 때문이다. 이 문서가 맡은 것은 인증 세 화면이고, 나머지는 건강기록·계정·판정 화면에 걸쳐 있다. 한 커밋에 한 변경이다.

### 13.2 `ckd` 라벨이 서버와 다르다

서버 `SPECS` 는 `"신기능 확인 필요"` 이고, 그 자리 주석이 "화면에서 'CKD'라 부르지 않는다 가 계약이다" 라고 적어 뒀다. 그런데 `frontend/src/features/assessment/contracts.ts:291` 은 아직 `"만성콩팥병"` 이다. 같은 주석이 "사용자가 두 이름을 다른 질환으로 읽었다(실제로 그 질문을 받았다)" 고 적은 그 상태로 보인다. 판정 화면 담당이 확인할 것.

### 13.3 확인해서 닫은 것

| 처음 의심 | 확인 결과 |
|---|---|
| 가입 카드가 말하는 "구독" 이 실재하는가 | 실재한다. `AccountPage.tsx:519` 에 구독 섹션이 있고 가정 구독 플랜·마스터 권한까지 붙어 있다. 문구를 고칠 것이 없다 |
| 판정이 의료 행위가 아니라는 고지가 있는가 | 있다. `AssessmentPage.tsx:1483` 이 서버가 보낸 `disclaimers` 를 그린다(`prediction.py:37` "의료 진단이 아닙니다…") |
